<%= render partial: 'application/errors', locals: {errors: @review.errors}%>
<%= @review.movie.title %>
<%= form_for([@review.movie, @review]) do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
<% end %>
